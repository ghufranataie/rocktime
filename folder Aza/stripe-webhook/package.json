{
  "name": "stripe-webhook",
  "version": "1.0.0",
  "description": "Lambda function to handle Stripe webhook events and save bookings to MySQL",
  "type": "module",
  "dependencies": {
    "stripe": "^14.0.0",
    "@aws-sdk/client-secrets-manager": "^3.0.0",
    "mysql2": "^3.6.0"
  }
}
